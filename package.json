{
  "name": "cust-instrument-check",
  "version": "1.0.0",
  "description": "Check if recent incomplete customer orders have stored instruments available by scanning a list of customer IDs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "fast-csv": "^4.1.3"
  }
}
